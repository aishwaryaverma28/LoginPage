import React from 'react'

const DashBoard = () => {
    const handleClick =() => {
        
    }
  return (
    <div>DashBoard
        <button onClick={handleClick}>Logout</button>
    </div>
  )
}

export default DashBoard